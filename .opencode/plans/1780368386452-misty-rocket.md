# Plan: Pulir Fase 1 — Base del Proyecto

**Generado**: 2026-06-01
**Complejidad Estimada**: Baja
**Tiempo Estimado**: 3-4 horas

## Visión General

Cerrar todos los cabos sueltos de la Fase 1 del MVP. La app ya compila y el CRUD de hermanos funciona, pero hay problemas de calidad que deben resolverse antes de construir el parser, las asignaciones y el S-140. Este plan es autocontenido: al terminarlo, la base queda sólida y lista para los siguientes sprints.

## Lo que está mal hoy

| # | Problema | Impacto |
|---|---|---|
| 1 | `index.html` tiene `lang="en"` y título genérico "Tauri + React + Typescript" | Baja calidad, parece template |
| 2 | `README.md` es el template default de Tauri | No describe el proyecto real |
| 3 | Tipos TypeScript duplicados: `Hermano` y `BatchInput` definidos por separado en `HermanosListPage.tsx` y `HermanoFormPage.tsx` | Riesgo de divergencia, código repetido |
| 4 | No existe comando `get_hermano` — para editar un hermano se cargan TODOS y se filtra en frontend | Ineficiente, no escala |
| 5 | No existe el enum `TipoAsignacion` en Rust | Requisito del PRD §4.2, necesario para el parser y la elegibilidad |
| 6 | Faltan `.docs/project-context.md` y `.docs/architecture.md` | Links rotos en `.docs/README.md` |
| 7 | Falta `tauri-plugin-dialog` | Necesario para importar PDFs (Sprint 2) |
| 8 | Ventana 800×600 | Muy chica para el S-140 |

---

## Tarea 1: Arreglar `index.html`

**Archivo**: `index.html`

**Qué hacer**:
- Línea 2: cambiar `lang="en"` → `lang="es"`
- Línea 7: cambiar `<title>Tauri + React + Typescript</title>` → `<title>Escuela — Asignaciones VyM</title>`

**Validación**: Abrir la app, verificar que la pestaña/ventana muestra el nuevo título.

---

## Tarea 2: Arreglar `README.md`

**Archivo**: `README.md`

**Qué hacer**: Reemplazar el contenido actual (7 líneas de template Tauri) por:

```markdown
# Escuela — Asignaciones VyM

Aplicación de escritorio local-first para gestionar el roster de la congregación y automatizar las asignaciones del programa semanal de la Reunión Vida y Ministerio Cristianos (formulario S-140).

## Stack

- **Plataforma**: Tauri 2 (Rust + WebView)
- **Frontend**: React 19 + TypeScript + Tailwind CSS v4 + React Router v7
- **Backend**: Rust + SQLite (rusqlite)
- **Parser PDF**: lopdf (Rust nativo)

## Desarrollo

```bash
pnpm install
pnpm tauri dev
```

## Documentación

Toda la documentación del proyecto está en [.docs/](.docs/).
```

**Validación**: `README.md` describe el proyecto real, no el template.

---

## Tarea 3: Extraer tipos TypeScript compartidos

### 3a — Crear `src/types.ts`

**Archivo**: `src/types.ts` (NUEVO)

**Qué hacer**: Crear el archivo con:

```typescript
export interface Hermano {
  id: number;
  nombre: string;
  sexo: string;
  rol: string;
  puede_presidir: boolean;
  puede_conducir_estudio: boolean;
  puede_ser_consejero_sala: boolean;
  activo: boolean;
  notas: string | null;
}

export interface BatchHermanoInput {
  nombre: string;
  sexo: string;
  rol: string;
  puede_presidir: boolean;
  puede_conducir_estudio: boolean;
  puede_ser_consejero_sala: boolean;
}

export const ROL_LABELS: Record<string, string> = {
  anciano: "Anciano",
  siervo_ministerial: "Siervo Ministerial",
  publicador: "Publicador",
  estudiante_biblia: "Estudiante de la Biblia",
};

export type Sexo = "masculino" | "femenino";
export type Rol = "anciano" | "siervo_ministerial" | "publicador" | "estudiante_biblia";
```

### 3b — Actualizar `HermanosListPage.tsx`

**Archivo**: `src/pages/HermanosListPage.tsx`

**Qué hacer**:
- Eliminar la interfaz `Hermano` local (líneas 5-15)
- Eliminar la constante `ROL_LABELS` local (líneas 17-22)
- Agregar: `import type { Hermano } from "../types";` y `import { ROL_LABELS } from "../types";`

### 3c — Actualizar `HermanoFormPage.tsx`

**Archivo**: `src/pages/HermanoFormPage.tsx`

**Qué hacer**:
- Eliminar la interfaz `Hermano` local (líneas 5-15)
- Eliminar la interfaz `BatchInput` local (líneas 17-24)
- Eliminar el tipo `FormMode` local (línea 26)
- Eliminar la función `makeEmptyRow` local (líneas 28-35) — mover a `types.ts` o dejarla como función exportada
- Agregar: `import type { Hermano, BatchHermanoInput } from "../types";`
- Renombrar todas las referencias a `BatchInput` → `BatchHermanoInput`

**Validación de la Tarea 3**: `tsc --noEmit` compila sin errores. Crear, editar, listar hermanos funciona exactamente igual que antes.

---

## Tarea 4: Agregar comando `get_hermano` (backend + frontend)

### 4a — Backend

**Archivo**: `src-tauri/src/commands/hermanos.rs`

**Qué hacer**: Agregar al final del archivo:

```rust
#[tauri::command]
pub fn get_hermano(
    db: State<'_, Database>,
    id: i64,
) -> Result<Hermano, String> {
    let conn = db.conn.lock().map_err(|e| e.to_string())?;

    conn.query_row(
        "SELECT id, nombre, sexo, rol, puede_presidir, puede_conducir_estudio,
                puede_ser_consejero_sala, activo, notas
         FROM hermanos WHERE id = ?1",
        rusqlite::params![id],
        |row| {
            Ok(Hermano {
                id: row.get(0)?,
                nombre: row.get(1)?,
                sexo: row.get(2)?,
                rol: row.get(3)?,
                puede_presidir: row.get::<_, i32>(4)? != 0,
                puede_conducir_estudio: row.get::<_, i32>(5)? != 0,
                puede_ser_consejero_sala: row.get::<_, i32>(6)? != 0,
                activo: row.get::<_, i32>(7)? != 0,
                notas: row.get(8)?,
            })
        },
    )
    .map_err(|e| e.to_string())
}
```

### 4b — Registrar en lib.rs

**Archivo**: `src-tauri/src/lib.rs`

**Qué hacer**: Agregar `commands::hermanos::get_hermano,` a la lista del `invoke_handler`.

### 4c — Frontend

**Archivo**: `src/pages/HermanoFormPage.tsx`

**Qué hacer**: Reemplazar la función `loadHermano` actual (líneas 56-75) que carga todos los hermanos con:

```typescript
const loadHermano = async (hermanoId: number) => {
  try {
    const found = await invoke<Hermano>("get_hermano", { id: hermanoId });
    setSingle({
      nombre: found.nombre,
      sexo: found.sexo,
      rol: found.rol,
      puede_presidir: found.puede_presidir,
      puede_conducir_estudio: found.puede_conducir_estudio,
      puede_ser_consejero_sala: found.puede_ser_consejero_sala,
    });
  } catch (err) {
    console.error("Error loading hermano:", err);
    navigate("/hermanos");
  }
};
```

Nota: si el hermano no existe, navega de vuelta al listado.

**Validación de la Tarea 4**: Editar un hermano carga solo sus datos. Si el ID no existe, redirige a `/hermanos`. `cargo check` limpio.

---

## Tarea 5: Crear enum `TipoAsignacion` en Rust

**Archivo**: `src-tauri/src/models.rs`

**Qué hacer**: Agregar después de los structs existentes:

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "snake_case")]
pub enum TipoAsignacion {
    DiscursoNoEstudiante,
    BusquemosPerlas,
    LecturaBiblia,
    EmpieceConversaciones,
    HagaRevisitas,
    HagaDiscipulos,
    ExpliqueCreenciasDiscurso,
    ExpliqueCreenciasEscenificacion,
    DiscursoEstudiante,
    AnalisisAuditorio,
    NecesidadesCongregacion,
    EstudioBiblico,
    Oracion,
    Cancion,
    Introduccion,
    Conclusion,
}

impl TipoAsignacion {
    pub fn seccion(&self) -> &'static str {
        match self {
            Self::DiscursoNoEstudiante | Self::BusquemosPerlas | Self::LecturaBiblia => "tesoros",
            Self::EmpieceConversaciones | Self::HagaRevisitas | Self::HagaDiscipulos
            | Self::ExpliqueCreenciasDiscurso | Self::ExpliqueCreenciasEscenificacion
            | Self::DiscursoEstudiante => "mejores_maestros",
            Self::AnalisisAuditorio | Self::NecesidadesCongregacion | Self::EstudioBiblico => "vida_cristiana",
            Self::Oracion | Self::Cancion | Self::Introduccion | Self::Conclusion => "marco",
        }
    }

    pub fn requiere_sala_auxiliar(&self) -> bool {
        matches!(
            self,
            Self::LecturaBiblia
                | Self::EmpieceConversaciones
                | Self::HagaRevisitas
                | Self::HagaDiscipulos
                | Self::ExpliqueCreenciasDiscurso
                | Self::ExpliqueCreenciasEscenificacion
                | Self::DiscursoEstudiante
        )
    }

    pub fn requiere_ayudante(&self) -> bool {
        matches!(
            self,
            Self::EmpieceConversaciones
                | Self::HagaRevisitas
                | Self::HagaDiscipulos
                | Self::ExpliqueCreenciasEscenificacion
        )
    }

    pub fn solo_varones(&self) -> bool {
        matches!(
            self,
            Self::LecturaBiblia | Self::ExpliqueCreenciasDiscurso | Self::DiscursoEstudiante
        )
    }

    pub fn solo_no_estudiante(&self) -> bool {
        matches!(
            self,
            Self::DiscursoNoEstudiante
                | Self::BusquemosPerlas
                | Self::AnalisisAuditorio
                | Self::NecesidadesCongregacion
                | Self::EstudioBiblico
        )
    }
}
```

**Validación**: `cargo check` compila. Los métodos retornan los valores correctos según la tabla del PRD §2.1 y §5.4.

---

## Tarea 6: Crear documentación `.docs/` faltante

### 6a — `project-context.md`

**Archivo**: `.docs/project-context.md` (NUEVO)

**Contenido**: Basado en PRD §1-3. Debe cubrir:
- **Propósito**: Automatizar asignaciones del programa semanal VyM
- **Usuario**: Superintendente de la Reunión Vida y Ministerio (usuario único, local-first)
- **Dominio**: Estructura de la reunión (3 secciones), reglas de elegibilidad (S-38), formulario S-140
- **Alcance MVP**: Roster, importación mwb, asignaciones con validación, S-140 imprimible
- **Fuera de alcance**: Rotación automática, multi-usuario, multi-congregación, integración JW.org

### 6b — `architecture.md`

**Archivo**: `.docs/architecture.md` (NUEVO)

**Contenido**: Debe cubrir:
- **Stack**: Tauri 2, React 19, TypeScript, Vite 7, Tailwind CSS v4, React Router v7, Rust, SQLite (rusqlite bundled), lopdf
- **Componentes**: Diagrama en texto de la arquitectura (Tauri App → Rust Backend → SQLite + React Frontend)
- **Modelo de datos**: Las 4 tablas (hermanos, semanas, partes, asignaciones) con columnas y relaciones
- **Comandos Tauri**: Lista completa de comandos implementados (hermanos) y planeados (semanas, partes, asignaciones, elegibilidad, parser)
- **Flujo del parser**: PDF → lopdf extrae texto → regex detecta semanas → classify_part_type → JSON → DB
- **Flujo de asignación**: Selector → get_eligible_brothers → assign_brother → DB → S140Page

### 6c — Actualizar `.docs/README.md`

**Archivo**: `.docs/README.md`

Ya se actualizó en la sesión anterior para incluir `master-plan.md`. Verificar que los links a `project-context.md` y `architecture.md` ahora funcionan (ya no estarán rotos).

**Validación de la Tarea 6**: Los 5 archivos en `.docs/` existen y son legibles. Ningún link está roto.

---

## Tarea 7: Agregar plugin dialog + ajustar ventana

### 7a — Dependencia Rust

**Archivo**: `src-tauri/Cargo.toml`

**Qué hacer**: Agregar en `[dependencies]`:
```toml
tauri-plugin-dialog = "2"
```

### 7b — Inicializar plugin

**Archivo**: `src-tauri/src/lib.rs`

**Qué hacer**: En el builder, agregar antes de `.invoke_handler(...)`:
```rust
.plugin(tauri_plugin_dialog::init())
```

### 7c — Permisos

**Archivo**: `src-tauri/capabilities/default.json`

**Qué hacer**: Agregar al array `permissions`:
```json
"dialog:default"
```

### 7d — Tamaño de ventana

**Archivo**: `src-tauri/tauri.conf.json`

**Qué hacer**: En `app.windows[0]`, cambiar:
```json
{
  "title": "Escuela — Asignaciones VyM",
  "width": 1200,
  "height": 800,
  "minWidth": 900,
  "minHeight": 600
}
```

**Validación de la Tarea 7**: `pnpm tauri dev` arranca sin errores. La ventana se abre a 1200×800. El diálogo de archivos estará disponible para el Sprint 2.

---

## Tarea 8: Validación integral

Ejecutar en orden y verificar cada punto:

- [ ] `cargo check` en `src-tauri/` — sin errores
- [ ] `tsc --noEmit` — sin errores
- [ ] `pnpm tauri dev` — arranca limpio
- [ ] Ventana se abre a 1200×800 con título "Escuela — Asignaciones VyM"
- [ ] Pestaña del navegador/webview muestra título correcto, `lang="es"`
- [ ] README.md describe el proyecto real
- [ ] Navegar a `/hermanos` → crear 2 hermanos (individual)
- [ ] Crear 3 hermanos más (batch mode)
- [ ] Listado muestra los 5 hermanos
- [ ] Editar un hermano → carga solo sus datos (sin consultar todos)
- [ ] Desactivar un hermano → desaparece del listado
- [ ] `.docs/` tiene 5 archivos: `README.md`, `project-context.md`, `architecture.md`, `ux-guidelines.md`, `master-plan.md`
- [ ] Links en `.docs/README.md` no están rotos
- [ ] `cargo check` compila con `TipoAsignacion` y `tauri-plugin-dialog`

---

## Orden de ejecución recomendado

Las tareas son mayormente independientes, pero se recomienda este orden:

1. **Tarea 1** (`index.html`) — 2 min, trivial
2. **Tarea 2** (`README.md`) — 5 min, trivial
3. **Tarea 3** (`types.ts` + refactor páginas) — 20 min, toca 3 archivos
4. **Tarea 4** (`get_hermano`) — 15 min, backend + frontend
5. **Tarea 5** (`TipoAsignacion` enum) — 15 min, solo Rust
6. **Tarea 7** (plugin dialog + window) — 10 min, config
7. **Tarea 6** (`.docs/`) — 20 min, solo escritura de docs
8. **Tarea 8** (validación) — 10 min

**Total**: ~1.5-2 horas de trabajo efectivo.

Las tareas 3, 4, 5, y 7 se pueden hacer en paralelo si se usan múltiples agentes (tocan archivos distintos sin conflictos). Las tareas 1, 2 y 6 son pura escritura y también son paralelizables.

---

## Rollback

Si algo falla:
- `git checkout -- .` revierte todos los cambios
- `git clean -fd` elimina archivos nuevos

Nada de lo que se hace en este plan es destructivo — son adiciones y refactors seguros.
